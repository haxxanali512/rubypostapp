<%= form_with(model: post) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :post_name , class:"form-label" %>
    <%= form.text_field :post_name,class:"form-control" %>
  </div>

  <div class="field">
  <%= form.label :post_description , class:"form-label" %>
    <%= form.text_field :description,class:"form-control" %>
  </div>
  <br>
  <div class="form-control">
 
  <% if post.photo.attached? %>
    <image src="<%= (url_for(@post.photo))%>" style="width: 200px; height: 200px;">
       <%end%>
       <br>
     <%= form.label :photo , class:"form-label" %>
    <%= form.file_field :photo, class:"form-control"%>  
  </div>
  <div class="field">
    <%= form.number_field :user_id, id: :post_user_id, class:"form-control", value:current_user.id, type: :hidden %>
  </div>

<br>
  <div class="actions">
    <%= form.submit class:'btn btn-secondary'%>
  </div>
  <br>

<% end %>

